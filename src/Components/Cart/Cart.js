import React from 'react';

const Cart = (props) => {
    const {cart}=props
    return (
        <div className='cart'>
            {/* <h3>{name}</h3> */}
        </div>
    );
};

export default Cart;